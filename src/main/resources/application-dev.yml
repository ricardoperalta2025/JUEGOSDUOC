spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_videojuegos_dev
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect